import { motion } from 'framer-motion'
import { getMode } from '../lib/modes'
const SUGGESTIONS = ['Explain photosynthesis like I am 10', 'Give me 3 study tips for finals', 'Write a short poem about curiosity', 'What is quantum entanglement?']
export default function Welcome({ mode, onSuggestion }) { const current = getMode(mode); const Icon = current.icon; return <div className="welcome"><motion.div className="welcome-orb" initial={{ scale: .7, opacity: 0 }} animate={{ scale: 1, opacity: 1 }} style={{ background: current.soft }}><div className="welcome-orb-inner" style={{ background: current.soft, color: current.accent }}><Icon size={28} /></div></motion.div><h1>How can I help you today?</h1><p>Your <span className="mode-badge" style={{ color: current.accent, background: current.soft }}>{current.label.toLowerCase()}</span> teacher is ready.</p><div className="suggestions">{SUGGESTIONS.map((suggestion) => <motion.button key={suggestion} onClick={() => onSuggestion(suggestion)} className="suggestion" whileHover={{ y: -2 }} whileTap={{ scale: .98 }}>{suggestion}</motion.button>)}</div></div> }
